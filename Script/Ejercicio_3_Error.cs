using UnityEngine;

public class Script_Ejercicio_3 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        transform.Translate(2, 1, 1);
    }

    // Update is called once per frame
    void Update()
    {
        
    }





}
